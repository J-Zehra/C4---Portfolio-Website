
import { extendTheme } from "@chakra-ui/react"

const colors = {
    palette: {
        primary: '#293241',
        secondary: '#252D3C',
        tertiary: '#E0FBFC',
        accent: '#3D5A80'
    }
}

export const theme = extendTheme({ colors });